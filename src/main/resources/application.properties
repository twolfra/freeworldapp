spring.application.name=app

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/marketplace
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update   # dev only; we?ll move to Flyway later
    properties:
      hibernate:
        format_sql: true
sql:
init:
mode: never
logging:
level:
org.hibernate.SQL: debug
org.hibernate.orm.jdbc.bind: trace
server:
port: 8080
